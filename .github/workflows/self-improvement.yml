name: Weekly Self-Improvement

on:
  schedule:
    # Every Monday at 9:00 AM UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:
    # Allow manual triggering

jobs:
  self-improvement:
    name: Self-Improvement Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm ci

      - name: Create Working Branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b self-improvement-$(date +%Y-%m-%d)

      - name: Gather Baseline Metrics
        run: |
          echo "=== Baseline Metrics ===" | tee metrics-baseline.txt
          echo "Date: $(date)" >> metrics-baseline.txt
          echo "" >> metrics-baseline.txt
          echo "File Sizes:" >> metrics-baseline.txt
          wc -l SKILL.md SKILL_PROFESSIONAL.md QWEN.md >> metrics-baseline.txt
          echo "" >> metrics-baseline.txt
          echo "AI Pattern Count:" >> metrics-baseline.txt
          grep -c -i "stands as\|testament to\|crucial\|pivotal\|vibrant\|showcasing" SKILL.md SKILL_PROFESSIONAL.md QWEN.md >> metrics-baseline.txt || echo "0" >> metrics-baseline.txt

      - name: Gather repository intelligence and decision support
        run: |
          node scripts/gather-repo-data.js edithatogo/humanizer-next blader/humanizer
          node scripts/render-self-improvement-issue.js

      - name: Run Validation
        run: |
          npm run validate
          npm test

      - name: Upload Baseline Metrics
        uses: actions/upload-artifact@v7
        with:
          name: baseline-metrics
          path: metrics-baseline.txt

      - name: Upload self-improvement data
        uses: actions/upload-artifact@v7
        with:
          name: self-improvement-data
          path: |
            conductor/tracks/repo-self-improvement_20260303/repo-data.json
            .github/generated/self-improvement-issue.md
            .github/generated/self-improvement-decisions.md

      - name: Create Analysis Issue
        uses: peter-evans/create-issue-from-file@v6
        with:
          title: Self-Improvement Cycle $(date +%Y-%m-%d)
          content-filepath: .github/generated/self-improvement-issue.md
          labels: |
            self-improvement
            weekly-cycle
            automated

      - name: Output Instructions
        run: |
          echo "::notice::Self-improvement cycle initiated. See issue created above for detailed analysis tasks and Adopt/Reject/Defer suggestions."
          echo "::notice::Branch created: self-improvement-$(date +%Y-%m-%d)"
          echo "::notice::Generated issue body, decision log, and repo-data.json are attached as workflow artifacts."
